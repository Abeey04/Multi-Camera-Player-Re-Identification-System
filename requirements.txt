opencv-python==4.8.1.78
numpy==1.24.3
ultralytics==8.0.196
scipy==1.10.1
scikit-image==0.20.0
cython-bbox==0.1.3
lap==0.4.0
matplotlib>=3.7.0
Pillow>=9.5.0
PyYAML>=6.0
requests>=2.31.0
tqdm>=4.65.0

# Optional: For enhanced performance
# torch>=2.0.0  # Uncomment if GPU acceleration is available
# torchvision>=0.15.0  # Uncomment if GPU acceleration is available

# Note: This requirements.txt ensures exact reproducibility
# For more flexibility, use >= instead of == for version specifications
